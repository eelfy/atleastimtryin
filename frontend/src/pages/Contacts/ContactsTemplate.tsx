import { FC } from 'react';
import { ContactsTemplateProps } from './ContactsTypes';

const ContactsTemplate: FC<ContactsTemplateProps> = ({

}) => (
  <div>contacts</div>
);

export default ContactsTemplate;
