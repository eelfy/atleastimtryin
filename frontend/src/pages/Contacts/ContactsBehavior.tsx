import { FC } from 'react';
import { ContactsBehaviorProps } from './ContactsTypes';
import ContactsTemplate from './ContactsTemplate';

const ContactsBehavior: FC<ContactsBehaviorProps> = ({}) => (
  <ContactsTemplate />
);

export default ContactsBehavior;
