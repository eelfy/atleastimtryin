import { FC } from 'react';
import { NotFoundBehaviorProps } from './NotFoundTypes';
import NotFoundTemplate from './NotFoundTemplate';

const NotFoundBehavior: FC<NotFoundBehaviorProps> = ({

}) => (
  <NotFoundTemplate />
);

export default NotFoundBehavior;
