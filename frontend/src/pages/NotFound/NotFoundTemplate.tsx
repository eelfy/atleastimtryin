import { FC } from 'react';
import { NotFoundTemplateProps } from './NotFoundTypes';

const NotFoundTemplate: FC<NotFoundTemplateProps> = ({

}) => (
  <div>not found</div>
);

export default NotFoundTemplate;
