import { FC } from 'react';
import { LoginBehaviorProps } from './LoginTypes';
import LoginTemplate from './LoginTemplate';

const LoginBehavior: FC<LoginBehaviorProps> = ({

}) => (
  <LoginTemplate />
);

export default LoginBehavior;
